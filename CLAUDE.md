# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository Status

This repository is currently a blank slate. As of the latest commit, it contains only a `README.md` with the title `# trading-bots` and no source code, build configuration, dependencies, tests, or tooling.

There is no language, framework, package manager, or directory structure established yet. Anything beyond the README must still be designed.

## Working in this repo

- Before assuming any conventions, re-check the working tree — once code is added, this file should be updated to describe the actual stack, commands, and architecture.
- When the user asks to scaffold the project, ask which language/runtime, package manager, and target exchange/broker integrations they want before generating files.
- Do not invent commands (build, test, lint) in responses — none exist until tooling is added.
